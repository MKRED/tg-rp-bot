// Мягкий лимит (дублируется в webapp для блокировки UI — здесь последняя линия защиты).
export const MAX_CARDS_PER_USER = 50;

// Максимум категорий структуры на карточку (как MAX_FIRST_MESSAGES у персонажа) — защита от абьюза.
export const MAX_CARD_CATEGORIES = 30;

// Лок карточки (cardLock.ts) и ожидание ответа на ask_user (pendingGeneration.ts) используют один
// TTL: если вкладка с открытым вопросом закрыта или потеряна сеть, через это время лок/ожидание
// можно переиспользовать — иначе карточка осталась бы занятой (не сохранить, не сгенерировать) навсегда.
export const CARD_LOCK_STALE_MS = 10 * 60 * 1000;
