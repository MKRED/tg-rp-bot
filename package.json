{
  "name": "tg-rp-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "bot",
    "webapp"
  ],
  "scripts": {
    "dev": "cd bot && tsx watch src/index.ts",
    "dev:web": "cd webapp && vite",
    "build": "cd bot && tsc && cd ../webapp && tsc --noEmit && vite build",
    "test": "cd bot && vitest run && cd ../webapp && vitest run",
    "test:watch": "cd bot && vitest"
  },
  "engines": {
    "node": ">=24"
  }
}
